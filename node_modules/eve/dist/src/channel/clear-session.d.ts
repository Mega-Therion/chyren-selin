import type { ClearFn } from "#channel/routes.js";
import type { Runtime } from "#channel/types.js";
/** Builds a channel-scoped context-clear helper. */
export declare function createClearFn(runtime: Runtime, channelName: string): ClearFn;

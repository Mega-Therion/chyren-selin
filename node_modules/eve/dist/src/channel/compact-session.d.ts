import type { CompactFn } from "#channel/routes.js";
import type { Runtime } from "#channel/types.js";
/** Builds a channel-scoped manual compaction helper. */
export declare function createCompactFn(runtime: Runtime, channelName: string): CompactFn;
